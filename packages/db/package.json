{
  "name": "@omniplug/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo \"No lint configured for @omniplug/db\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured for @omniplug/db\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "prisma": "5.14.0",
    "typescript": "5.4.5"
  }
}
